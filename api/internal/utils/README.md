# internal/utils

Utilidades compartidas de la capa `internal`.

Nota: este directorio es parte de la nueva estructura vertical por dominios. Las utilidades existentes en `pkg/` se mantienen como paquetes compartidos reutilizables entre módulos.
